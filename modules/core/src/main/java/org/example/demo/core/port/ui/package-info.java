/**
 * To separate platform-specific dependencies from core of the application,<p>
 * almost all GUI element represented only as interfaces or abstract classes<p>
 * For implementation details check 'javafx' module
 * <p>
 * But looks like I overdo with interfaces;)
 * //TODO: Combine some interfaces to decrease its number
 */
package org.example.demo.core.port.ui;