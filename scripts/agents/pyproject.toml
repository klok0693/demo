[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-playground"
version = "0.1.0"
description = "Local multi-agent experiments"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]

